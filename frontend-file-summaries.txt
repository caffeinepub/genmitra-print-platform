{
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; `useGetProduct` now skips the backend call for `demo-` prefixed IDs (returning null immediately so the page falls back to demo data), while all other hooks remain unchanged.",
  "frontend/src/components/FAQSection.tsx": "Accordion-style FAQ section with two-column layout and smooth expand/collapse animations. Fixed `flex-shrink-0` → `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/components/Footer.tsx": "Professional 4-column ecommerce footer with store locations, company links, support links, contact info (updated to care@genmitra.in), social icons, payment icons, copyright updated to \"© {year} GenMitra. All Rights Reserved.\", and caffeine.ai attribution.",
  "frontend/src/components/Layout.tsx": "Main site layout with sticky header, promo bar, category navigation, cart icon, user dropdown, and footer; logo alt text and fallback text updated from \"PrintCraft Studio\"/\"PrintCraft\" to \"GenMitra\".",
  "frontend/src/pages/admin/AdminSettingsPage.tsx": "Admin settings page with delivery pincode management (add/remove pincodes), access control information display, and platform information. Provides forms to add and remove delivery pincodes with local session tracking, plus informational sections about roles and platform details.",
  "frontend/src/App.tsx": "Main app component with TanStack Router setup; fixes the route crash by removing the conflicting `id: 'admin-layout'` from `adminLayoutRoute` (which already has `path: '/admin'`) — a route cannot have both `id` and `path` simultaneously. All public and admin routes remain intact.",
  "frontend/src/pages/HomePage.tsx": "Home page that uses `useSearch({ from: '/layout/' })` to read the active category filter, rendering either the full page sections or a filtered product listing; the TrustFeatures import and usage have been removed.",
  "frontend/src/components/FeaturedProducts.tsx": "Product grid component with demo data fallback using the correctly named `demoProducts` export (not the non-existent `getDemoProducts`); all image src values pass through `getImageSrc` to prevent malformed URLs.",
  "frontend/src/components/ProductCategoryGrid.tsx": "Category grid with five horizontal cards using lucide-react icons; no image src changes needed (uses only icon components), preserved existing navigation behavior.",
  "frontend/src/pages/admin/TemplateManagementPage.tsx": "Admin template management page with create/edit/delete functionality; all template image and preview image src values pass through `getImageSrc`.",
  "frontend/src/pages/admin/ProductManagementPage.tsx": "Admin product management page with create/edit/delete functionality; all product image and template image src values pass through `getImageSrc` in both the list and modal preview.",
  "frontend/src/pages/CartPage.tsx": "Multi-step checkout page (cart → address → payment → success); all cart item product images and custom images now pass through `getImageSrc` to prevent malformed URLs.",
  "frontend/src/pages/EditorPage.tsx": "Redesigned photo editor page with a clean light-gray full-page background, a top bar with portrait/landscape orientation toggles (teal filled active state) and a teal \"Save & Select Size →\" button, a centered brown-bordered frame mockup with white interior, drag-and-drop upload placeholder, and a horizontal icon toolbar (Upload, ZoomIn, ZoomOut, Rotate, AddText) below the frame. Preserves all existing editor capabilities: drag-to-reposition, scroll/button zoom, 90° rotation, text overlay, and save-to-cart integration.",
  "frontend/src/pages/OrderDetailPage.tsx": "Order detail/tracking page with status timeline, items, shipping address, and payment info; all order item product images and custom images pass through `getImageSrc`.",
  "frontend/src/pages/ProductDetailPage.tsx": "Product detail page with size selector, pincode check, and upload/cart actions; both `product.imageData` and `product.templateImageData` now pass through `getImageSrc` before rendering.",
  "frontend/src/pages/admin/OrderManagementPage.tsx": "Admin order management page with status filtering, order expansion, and production file management; fixes the `useUpdateOrderStatus` mutation call to use `status` instead of the incorrect `newStatus` key.",
  "frontend/src/components/HeroBanner.tsx": "Hero banner section with background image, headline, CTA buttons, and stats; fixed `navigate({ to: '/' })` calls to include `search: { category: undefined }` to satisfy the required search param.",
  "frontend/src/components/ShopFramesBySize.tsx": "Frame size cards section displaying 8 size options; all image src values pass through `getImageSrc` utility.",
  "frontend/src/components/admin/AdminLayout.tsx": "Admin sidebar layout with navigation and logout; sidebar logo/brand name updated from \"PrintCraft\" to \"GenMitra\".",
  "frontend/src/pages/CheckoutPage.tsx": "Multi-step checkout page with address form, payment selection, and order summary; fixed `navigate({ to: '/cart' })` to include required `search: { category: undefined }` param.",
  "frontend/src/pages/OrderConfirmationPage.tsx": "Order confirmation page shown after successful order placement; fixes `useParams` `from` path to use the correct full route ID `/layout/order-confirmation/$orderId`, resolving both the type error and the `never` type on `orderId`.",
  "frontend/src/pages/LoginPage.tsx": "Login page supporting both admin credential mode and Internet Identity mode; fixes the `/create-account` navigation to include the required `search: { category: undefined }` param.",
  "frontend/src/pages/CreateAccountPage.tsx": "Account creation page with username/email fields and Internet Identity login; fixed `navigate({ to: '/login' })` to include required `search: { mode: undefined, redirect: undefined }` params.",
  "frontend/src/pages/ForgotPasswordPage.tsx": "Forgot password page with email input and success state; fixed all three `navigate({ to: '/login' })` calls to include required `search: { mode: undefined, redirect: undefined }` params.",
  "frontend/src/pages/MyOrdersPage.tsx": "User orders listing page with status badges and navigation to order details; fixed `navigate` to `/order/$orderId` to include required `search: { category: undefined }` param.",
  "frontend/src/components/admin/AdminGuard.tsx": "Route guard that checks the local credential-based admin session (no Internet Identity dependency); unauthenticated visitors are redirected to `/login?mode=admin&redirect=<current-path>`, and authenticated admins trigger a backend re-authentication on page refresh before rendering protected routes.",
  "frontend/src/hooks/useAdminSession.ts": "Admin session hook using sessionStorage that validates credentials against two hardcoded admin accounts (genmitra/12345678 and admin/Admin@1234) before setting the session; `loginAdmin` now returns a boolean indicating success or failure.",
  "frontend/tailwind.config.js": "Extended Tailwind config adding `category` color tokens (bg, heading, subtitle, icon, icon-bg, icon-bg-hover) and `category`/`category-hover` box-shadow utilities to support the redesigned ProductCategoryGrid; also aliases `serif` to Playfair Display.",
  "frontend/src/lib/demoProducts.ts": "Shared demo product data module exporting 15 typed fallback products across all five categories (Photo Prints, Photo Frames, Photo Magnets, Mugs, Corporate Gifts — 3 each), all referencing newly generated sample images under `/assets/generated/`, plus a `getDemoProductById` helper.",
  "frontend/src/components/FrameStyleDropdown.tsx": "Custom visual dropdown component for selecting photo frame styles; shows a corner-preview thumbnail alongside each style name (Plain Black, Plain Brown, Goldline Brown, Gold Sash Brown, Zigzag Brown), highlights the selected option with a purple background, and closes on outside click.",
  "frontend/src/hooks/useAuth.ts": "Simplified auth hook that combines Internet Identity state with user profile query; removed references to non-existent `useGetCallerUserRole` and `useIsCallerAdmin` exports.",
  "frontend/src/utils/imageHelpers.ts": "New utility module exporting `getImageSrc` that safely returns image src strings — returning data URIs and HTTP/HTTPS URLs unchanged, and only prepending the base64 prefix for raw base64 strings, preventing the erroneous `data:image/jpeg;base64,https://...` pattern.",
  "frontend/src/pages/admin/AdminDashboardPage.tsx": "Admin dashboard with stats cards and recent orders table; product images in the recent orders list now pass through `getImageSrc`.",
  "frontend/index.html": "HTML entry point with a Content-Security-Policy meta tag added to allow framing from `https://sole-aqua-rc6-draft.caffeine.xyz` and all `*.caffeine.xyz` / `*.caffeine.ai` origins, fixing the disallowed origin error in the Caffeine preview environment."
}