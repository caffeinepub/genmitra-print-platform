{
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions; includes useGetCallerUserRole, useIsCallerAdmin, useAssignUserRole, and all CRUD mutation hooks for products, templates, orders, cart, pincodes, and addresses. All six mutation hooks (createTemplate, updateTemplate, deleteTemplate, createProduct, updateProduct, deleteProduct) obtain their actor directly from useActor() with no anonymous fallback, throwing an error if the actor is unavailable.",
  "frontend/src/components/FAQSection.tsx": "Accordion-style FAQ section with two-column layout and smooth expand/collapse animations. Fixed `flex-shrink-0` → `shrink-0` for Tailwind v3 compatibility.",
  "frontend/src/components/Footer.tsx": "Professional 4-column ecommerce footer with store locations, company links, support links, contact info, social icons, payment icons, and attribution. Fixed `flex-shrink-0` → `shrink-0` for Tailwind v3 compatibility, and removed unused `Link` import.",
  "frontend/src/components/Layout.tsx": "Main site layout with sticky header, promo bar, category navigation, cart icon, user dropdown, and footer; fixed `navigate({ to: '/', search: {} })` to use `search: { category: undefined }`.",
  "frontend/src/pages/admin/AdminSettingsPage.tsx": "Admin settings page with delivery pincode management (add/remove pincodes), access control information display, and platform information. Provides forms to add and remove delivery pincodes with local session tracking, plus informational sections about roles and platform details.",
  "frontend/src/App.tsx": "Main app component with TanStack Router setup; admin layout route now wraps `AdminGuard` around `AdminLayout` to enforce authentication and admin-role checks before rendering any `/admin/*` route.",
  "frontend/src/pages/HomePage.tsx": "Home page that reads the `category` search param via `useSearch` and conditionally renders the full page sections (hero, grid, etc.) or just the filtered `FeaturedProducts` when a category is active.",
  "frontend/src/components/FeaturedProducts.tsx": "Product grid component with demo fallback data; each product card is now fully clickable and navigates to `/product/$productId` using TanStack Router's `useNavigate`, with the \"Add to Cart\" and like buttons using `stopPropagation` to prevent card navigation when clicked.",
  "frontend/src/components/ProductCategoryGrid.tsx": "Category grid showing all 5 product categories with their dedicated demo images; clicking a category now correctly navigates to `/?category=<name>` so the FeaturedProducts section filters and displays the relevant demo products.",
  "frontend/src/pages/admin/TemplateManagementPage.tsx": "Admin template management page with canvas-based image compression (max 800px, JPEG 0.75) for both template image and preview image fields; form modal with proper base64 string handling passed directly to createTemplate/updateTemplate backend calls, with success and error toasts on mutation results.",
  "frontend/src/pages/admin/ProductManagementPage.tsx": "Admin product management page with canvas-based image compression (max 800px, JPEG 0.75) for both product image and template overlay image fields; form modal with proper base64 string handling passed directly to createProduct/updateProduct backend calls, with success and error toasts on mutation results.",
  "frontend/src/pages/CartPage.tsx": "Shopping cart page with multi-step checkout (cart → address → payment → success); updated to use `imageData`/`templateImageData` field names from the new backend interface.",
  "frontend/src/pages/EditorPage.tsx": "Photo editor page with canvas-based image editing, template overlay, zoom/pan controls, and add-to-cart; updated demo product to use `imageData`/`templateImageData` fields and `customImageData` for cart items.",
  "frontend/src/pages/OrderDetailPage.tsx": "Order detail/tracking page that reads `orderId` from TanStack Router route params (via `useParams`) instead of component props, fixing the router type incompatibility; displays order status timeline, items, shipping address, and payment info.",
  "frontend/src/pages/ProductDetailPage.tsx": "Product detail page with image, info, size/quantity selection, and add-to-cart/customize actions; fixed `navigate({ to: '/' })` to include `search: { category: undefined }`.",
  "frontend/src/pages/admin/OrderManagementPage.tsx": "Admin order management page with status filtering, order expansion, custom image download, and production file generation/download; updated all references from `customImageUrl`/`productionFileUrl` to `customImageData`/`productionFileData` and uses `useSetProductionFileData` hook.",
  "frontend/src/components/HeroBanner.tsx": "Hero banner section with background image, headline, CTA buttons, and stats; fixed `navigate({ to: '/' })` calls to include `search: { category: undefined }` to satisfy the required search param.",
  "frontend/src/components/ShopFramesBySize.tsx": "Size selector grid component; fixed `navigate({ to: '/' })` to include `search: { category: undefined }`.",
  "frontend/src/components/admin/AdminLayout.tsx": "Admin sidebar layout with navigation and logout; updated to use `useAdminSession().logoutAdmin()` instead of `useAuth()` so logout clears the local admin session without touching Internet Identity.",
  "frontend/src/pages/CheckoutPage.tsx": "Multi-step checkout page with address form, payment method selection, and order summary; replaced `navigate` to unregistered `/order-confirmation/$orderId` with `window.location.href` redirect.",
  "frontend/src/pages/OrderConfirmationPage.tsx": "Order confirmation page showing order details after successful placement; fixed `useParams` `from` path, replaced `Link`/`navigate` to unregistered routes with button-based navigation using valid routes.",
  "frontend/src/pages/LoginPage.tsx": "Login page that now calls `actor.login(username, password)` on the backend for admin mode (assigning the admin role to the caller's principal via AccessControl) before setting the local session flag and redirecting to `/admin`.",
  "frontend/src/pages/CreateAccountPage.tsx": "Account creation page with username/email fields and Internet Identity login; fixed `navigate({ to: '/login' })` to include required `search: { mode: undefined, redirect: undefined }` params.",
  "frontend/src/pages/ForgotPasswordPage.tsx": "Forgot password page with email input and success state; fixed all three `navigate({ to: '/login' })` calls to include required `search: { mode: undefined, redirect: undefined }` params.",
  "frontend/src/pages/MyOrdersPage.tsx": "My orders page listing user orders with status badges; fixed `navigate({ to: '/' })` to include `search: { category: undefined }`.",
  "frontend/src/components/admin/AdminGuard.tsx": "Route guard that checks the local admin session and, when the actor is ready, re-calls `actor.login()` with stored credentials to restore the backend admin role after page refresh before rendering protected admin routes.",
  "frontend/src/hooks/useAdminSession.ts": "Admin session hook using sessionStorage that now also stores/retrieves admin credentials (username + password) so they can be used to re-authenticate with the backend on page refresh."
}